import 'package:flutter/material.dart';

const double kTextSize = 12;
const double kTextHeaderSize = 16;
const double kTextTitleSize = 32;
const Color kError = Colors.red;

const kTextStyle = TextStyle(color: Colors.black);
const kTitleStyle = TextStyle(fontSize: kTextHeaderSize);

const kBorderTitleStyle =
    TextStyle(color: Colors.black, fontSize: kTextHeaderSize);
